# Инструкция по скачиванию и подготовке датасета

## 📥 Способ 1: Ручное скачивание (рекомендуется)

### Шаг 1: Скачать из Google Drive

1. Откройте ссылку: https://drive.google.com/drive/folders/1xS3Hu_s-uqtVHdy7n0Y2t_Dv2RxkdCYj
2. Найдите папку **"named"** (содержит дорожные знаки с названиями)
3. Нажмите правой кнопкой на папку → **Скачать**
4. Google Drive создаст ZIP архив - дождитесь скачивания
5. Распакуйте архив в удобное место (например, `~/Downloads/named`)

### Шаг 2: Организовать файлы

```bash
# Перейти в директорию проекта
cd /path/to/RSD

# Запустить скрипт организации
python3 utils/organize_downloaded.py \
    --input ~/Downloads/named \
    --output dataset/raw

# Скрипт автоматически:
# - Распознает типы знаков по именам файлов
# - Переименует в стандартный формат
# - Создаст class_mapping.json
# - Выведет статистику
```

### Шаг 3: Обновить конфигурацию

Скопируйте классы из вывода скрипта в `configs/dataset.yaml`:

```yaml
names:
  0: bicycle_crossing
  1: children_crossing
  2: construction
  # ... остальные классы
nc: 20  # количество классов
```

---

## 📥 Способ 2: Автоматическое скачивание (требует gdown)

### Установка gdown

```bash
# Вариант 1: через pipx (рекомендуется для macOS)
brew install pipx
pipx install gdown

# Вариант 2: через virtual environment
python3 -m venv venv
source venv/bin/activate
pip install gdown

# Вариант 3: system-wide (не рекомендуется)
python3 -m pip install --break-system-packages gdown
```

### Скачивание

```bash
python3 utils/download_dataset.py \
    --output dataset/raw \
    --folder-id 1xS3Hu_s-uqtVHdy7n0Y2t_Dv2RxkdCYj
```

---

## 📊 Что делает скрипт организации?

### Распознавание типов знаков

Скрипт анализирует названия файлов и автоматически определяет тип знака:

**Примеры распознавания:**
- `speed_limit_50_01.jpg` → `speed_limit_50_0001.jpg`
- `pedestrian_crossing.png` → `pedestrian_crossing_0001.png`
- `3.20_no_overtaking.jpg` → `no_overtaking_0001.jpg` (по номеру ГОСТ)
- `Знак_Дети.jpg` → `children_crossing_0001.jpg` (русские названия)

### Поддерживаемые категории

**Ограничения скорости:**
- speed_limit_20/30/50/60/70/80/100/120

**Запрещающие знаки:**
- no_entry (въезд запрещен)
- stop_sign (стоп)
- no_overtaking (обгон запрещен)
- no_parking (стоянка запрещена)

**Предупреждающие знаки:**
- pedestrian_crossing (пешеходный переход)
- children_crossing (дети)
- bicycle_crossing (велосипедисты)
- slippery_road (скользкая дорога)
- construction (дорожные работы)
- traffic_light (светофор)
- dangerous_curve (опасный поворот)

**Знаки приоритета:**
- priority_road (главная дорога)
- yield_sign (уступите дорогу)
- stop_sign (стоп)

**Предписывающие знаки:**
- roundabout (круговое движение)
- turn_left/turn_right (поворот налево/направо)
- go_straight (только прямо)

**Информационные:**
- parking (парковка)

---

## 🔍 Проверка результата

После организации файлов:

```bash
# Просмотреть статистику
ls -lh dataset/raw/ | wc -l   # Количество файлов

# Проверить class_mapping
cat dataset/raw/class_mapping.json

# Проверить примеры файлов
ls dataset/raw/ | head -20
```

**Ожидаемая структура:**
```
dataset/raw/
├── speed_limit_50_0001.jpg
├── speed_limit_50_0002.jpg
├── stop_sign_0001.jpg
├── pedestrian_crossing_0001.jpg
├── ...
└── class_mapping.json
```

---

## ⚠️ Важные замечания

### Файлы без распознанного типа

Если файл не был распознан, он получит префикс `unknown_`:
```
unknown_0001.jpg
```

**Решение:**
1. Проверьте имя оригинального файла
2. Переименуйте вручную в формат: `тип_знака_номер.jpg`
3. Или обновите паттерны в `utils/organize_downloaded.py`

### Если датасет очень большой (>10k файлов)

```bash
# Разбить на батчи
python3 utils/organize_downloaded.py \
    --input ~/Downloads/named/batch1 \
    --output dataset/raw

python3 utils/organize_downloaded.py \
    --input ~/Downloads/named/batch2 \
    --output dataset/raw
# Файлы автоматически продолжат нумерацию
```

---

## 🎯 Следующие шаги

После организации файлов:

### 1. Создать YOLO аннотации

Если у вас ещё нет `.txt` файлов с аннотациями (bounding boxes):

**Вариант A: LabelImg (рекомендуется)**
```bash
pip install labelImg
labelImg dataset/raw configs/dataset.yaml
```

**Вариант B: CVAT (для командной работы)**
- https://cvat.org

**Вариант C: Roboflow**
- https://roboflow.com

### 2. Валидировать датасет

```bash
python3 utils/prepare_dataset.py \
    --input dataset/raw \
    --output dataset \
    --validate
```

### 3. Начать обучение

```bash
python3 training/train.py \
    --data configs/dataset.yaml \
    --epochs 200 \
    --batch 16
```

---

## 💡 Полезные советы

### Как добавить новые типы знаков

Отредактируйте `utils/organize_downloaded.py`, добавьте паттерн:

```python
SIGN_PATTERNS = {
    # Добавьте свой паттерн
    r'(?:ваш_паттерн|alternative)': 'your_sign_type',
    # Существующие паттерны...
}
```

### Массовое переименование

Если скачали файлы с плохими именами:

```bash
# Пример: добавить префикс всем файлам
cd ~/Downloads/named
for f in *.jpg; do 
    mv "$f" "speed_limit_50_$f"
done
```

### Проверка качества изображений

```bash
# Найти слишком маленькие изображения (<100KB)
find dataset/raw -name "*.jpg" -size -100k

# Найти поврежденные изображения
python3 -c "
import cv2
from pathlib import Path
for img in Path('dataset/raw').glob('*.jpg'):
    if cv2.imread(str(img)) is None:
        print(f'Поврежден: {img}')
"
```

---

## 🆘 Решение проблем

### "Не могу скачать из Google Drive"
- Проверьте что ссылка публичная
- Попробуйте другой браузер
- Используйте режим инкогнито
- Скачайте через мобильное приложение

### "Скрипт не находит файлы"
```bash
# Проверьте путь
ls -la ~/Downloads/named
# Убедитесь что используете правильный путь
```

### "Много файлов с типом 'unknown'"
- Проверьте имена файлов - они должны содержать описание знака
- Обновите паттерны в скрипте под ваши данные
- Переименуйте файлы вручную в понятный формат

---

Готово! После выполнения этих шагов у вас будет организованный датасет, готовый для аннотирования и обучения.
